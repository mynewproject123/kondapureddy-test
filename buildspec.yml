version: 0.2
phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
      - echo "Testing command"
  build:
    commands:
      - echo "Build phase"
  post_build:
    commands:
      - echo "Post-build phase"
artifacts:
  files:
    - appspec.yaml
    - taskdef.json 
